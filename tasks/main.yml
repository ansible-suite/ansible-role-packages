---
- name: Install packages
  ansible.builtin.package:
    name: "{{ packages | flatten }}"
    state: present
  when: packages | length > 0

- name: Remove packages
  ansible.builtin.package:
    name: "{{ packages_remove | flatten }}"
    state: absent
  when: packages_remove | length > 0
